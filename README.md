# Knowledge Base — .NET 8 WPF Client + TCP Venues (EURIBOR)

This knowledge pack provides **functional and technical specs** to generate a .NET 8 solution:
- WPF client (MVVM, modern cards, blotters)
- 3 TCP console services (`Venue_A`, `Venue_B`, `Venue_C`) streaming prices with **different intervals**
- Direct **TCP** request/response for orders (no message bus)

Use this as **Custom Knowledge** in a GPT to guide code generation and delivery (single ZIP).