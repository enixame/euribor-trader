<Window x:Class="Trading.Client.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Trading.Client.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="EURIBOR Trader" Height="600" Width="900">
    <Window.Resources>
        <!-- Data template for product tiles -->
        <DataTemplate DataType="{x:Type vm:ProductTileViewModel}">
            <Border Padding="10" Margin="5" CornerRadius="6" BorderBrush="Gray" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" FontSize="16"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                        <TextBlock Text="Best Bid: "/>
                        <TextBlock Text="{Binding BestBid, StringFormat=F3}" Margin="2,0,12,0"/>
                        <TextBlock Text="Best Ask: "/>
                        <TextBlock Text="{Binding BestAsk, StringFormat=F3}" Margin="2,0,0,0"/>
                    </StackPanel>
                    <ItemsControl ItemsSource="{Binding Venues}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <DockPanel LastChildFill="False" Margin="0,0,0,2">
                                    <TextBlock Text="{Binding Venue}" Width="80"/>
                                    <TextBlock Text="{Binding Bid, StringFormat=F3}" Width="70">
                                        <TextBlock.Style>
                                            <Style TargetType="{x:Type TextBlock}">
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsBestBid}" Value="True">
                                                        <Setter Property="Foreground" Value="Green"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Ask, StringFormat=F3}" Width="70">
                                        <TextBlock.Style>
                                            <Style TargetType="{x:Type TextBlock}">
                                                <Setter Property="Foreground" Value="Black"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsBestAsk}" Value="True">
                                                        <Setter Property="Foreground" Value="Red"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding AgeMs, StringFormat=\{0:0\}ms}" Width="60"/>
                                </DockPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <Button Content="Buy @ Best" Command="{Binding BuyBestCommand}" Margin="0,0,8,0"/>
                        <Button Content="Sell @ Best" Command="{Binding SellBestCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8">
            <Button Content="Toggle Live (F5)" Width="120" Command="{Binding ToggleLiveCommand}"/>
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0}">
                        <Binding Path="IsLive"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <!-- Product tiles area -->
            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Products}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
            <!-- Orders blotter -->
            <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" AutoGenerateColumns="False" Margin="0,8,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding CreatedUtc, StringFormat=HH:mm:ss}" Width="80"/>
                    <DataGridTextColumn Header="Product" Binding="{Binding ProductId}" Width="90"/>
                    <DataGridTextColumn Header="Venue" Binding="{Binding Venue}" Width="80"/>
                    <DataGridTextColumn Header="Side" Binding="{Binding Side}" Width="60"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=F3}" Width="80"/>
                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="80"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                    <DataGridTextColumn Header="Matched" Binding="{Binding MatchedPrice, StringFormat=F3}" Width="80"/>
                    <DataGridTextColumn Header="Reason" Binding="{Binding RejectReason}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>